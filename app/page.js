'use client';
// Root page — always shows the landing page
// Marketplace lives at /market
export default function HomePage() {
  return null; // Landing is rendered by ClientLayout when pathname === '/'
}
