{
  "name": "gemline-backend",
  "version": "1.0.0",
  "description": "GEMLINE trading card exchange — settlement engine + arbitrage feed backend",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/lifecycle.test.js",
    "migrate": "psql \"$DATABASE_URL\" -f db/schema.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "optionalDependencies": {
    "pg": "^8.22.0"
  },
  "engines": {
    "node": ">=18"
  }
}
